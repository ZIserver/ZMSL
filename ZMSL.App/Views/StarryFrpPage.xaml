<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ZMSL.App.Views.StarryFrpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:ZMSL.App.Models"
    xmlns:converters="using:ZMSL.App.Converters"
    Loaded="Page_Loaded">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </Page.Resources>

    <ScrollViewer Padding="40,32">
        <StackPanel Spacing="20">
            <StackPanel Spacing="6">
                <TextBlock Text="StarryFrp" FontSize="32" FontWeight="Bold"/>
            </StackPanel>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="16">
                        <TextBlock Text="客户端与登录" FontSize="18" FontWeight="SemiBold"/>
                        <InfoBar IsOpen="True" IsClosable="False" Severity="Informational"
                                 Title="说明"
                                 Message="首次使用先安装客户端，再输入 Token 登录。Token 每次打开页面都会自动校验，失效会自动清除。"/>

                        <StackPanel>
                            <TextBlock Text="访问令牌 Token" Opacity="0.7" Margin="0,0,0,6"/>
                            <PasswordBox x:Name="TokenBox" PlaceholderText="请输入 StarryFrp 访问令牌" PasswordRevealMode="Visible"/>
                        </StackPanel>

                        <ProgressBar Value="{x:Bind ViewModel.DownloadProgress, Mode=OneWay}" Maximum="100"
                                     Visibility="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"/>
                        <TextBlock Text="{x:Bind ViewModel.DownloadStatus, Mode=OneWay}" Opacity="0.7"/>
                        <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button Content="安装客户端" Style="{StaticResource AccentButtonStyle}" Click="Install_Click"/>
                            <Button Content="登录" Click="Login_Click"/>
                            <Button Content="刷新" Click="Refresh_Click"/>
                            <Button Content="退出登录" Click="Logout_Click"/>
                        </StackPanel>
                    </StackPanel>

                    <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="10" Padding="20">
                        <StackPanel Spacing="12">
                            <TextBlock Text="状态信息" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind ViewModel.InstallStateText, Mode=OneWay}"/>
                            <TextBlock Text="{x:Bind ViewModel.LoginStateText, Mode=OneWay}"/>
                            <StackPanel Spacing="4">
                                <TextBlock Text="当前运行隧道" Opacity="0.7"/>
                                <TextBlock Text="{x:Bind ViewModel.RunningTunnelText, Mode=OneWay}" FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="客户端：frpc/starryfrp.exe" Opacity="0.7"/>
                            <TextBlock Text="配置：starryfrp.toml" Opacity="0.7"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="20" Margin="0,0,8,0">
                    <StackPanel Spacing="8"><TextBlock Text="名字" Opacity="0.7"/><TextBlock Text="{x:Bind ViewModel.UserNameText, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="20" Margin="8,0">
                    <StackPanel Spacing="8"><TextBlock Text="邮箱" Opacity="0.7"/><TextBlock Text="{x:Bind ViewModel.UserEmailText, Mode=OneWay}" TextWrapping="Wrap" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="20" Margin="8,0">
                    <StackPanel Spacing="8"><TextBlock Text="实名状态" Opacity="0.7"/><TextBlock Text="{x:Bind ViewModel.RealnameStatusText, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Grid.Column="3" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="20" Margin="8,0">
                    <StackPanel Spacing="8"><TextBlock Text="隧道数量" Opacity="0.7"/><TextBlock Text="{x:Bind ViewModel.ProxyCountText, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/></StackPanel>
                </Border>
                <Border Grid.Column="4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="20" Margin="8,0,0,0">
                    <StackPanel Spacing="8"><TextBlock Text="剩余流量" Opacity="0.7"/><TextBlock Text="{x:Bind ViewModel.TrafficText, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/></StackPanel>
                </Border>
            </Grid>

            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="12" Padding="20">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="6">
                        <TextBlock FontSize="18" FontWeight="SemiBold">
                            <Run Text="当前运行隧道："/>
                            <Run Text="{x:Bind ViewModel.RunningTunnelText, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Text="访问地址" Opacity="0.7"/>
                        <TextBlock Text="{x:Bind ViewModel.CurrentAccessEndpoint, Mode=OneWay}" IsTextSelectionEnabled="True" TextWrapping="Wrap" FontFamily="Consolas"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="停止隧道" Padding="16,8" Click="StopTunnel_Click" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="隧道列表" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="创建隧道" Style="{StaticResource AccentButtonStyle}" Click="OpenCreateDialog_Click"/>
                    </Grid>
                    <ListView Grid.Row="1" Margin="0,14,0,0" ItemsSource="{x:Bind ViewModel.Proxies, Mode=OneWay}" SelectionChanged="ProxyList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:StarryFrpProxy">
                                <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="10" Padding="16" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Spacing="6">
                                            <TextBlock Text="{x:Bind ProxyName}" FontSize="16" FontWeight="SemiBold"/>
                                            <TextBlock Text="{x:Bind Remark}" Opacity="0.78" TextWrapping="Wrap"/>
                                            <TextBlock><Run Text="协议："/><Run Text="{x:Bind ProxyType}"/></TextBlock>
                                            <TextBlock><Run Text="本地："/><Run Text="{x:Bind LocalIp}"/><Run Text=":"/><Run Text="{x:Bind LocalPort}"/></TextBlock>
                                            <TextBlock><Run Text="远程端口："/><Run Text="{x:Bind RemotePort}"/></TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="启动隧道" Tag="{x:Bind ProxyId}" Click="StartTunnel_Click" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="12" Padding="20" MaxHeight="360">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="4">
                            <TextBlock Text="运行日志" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{x:Bind ViewModel.LogTargetTitle, Mode=OneWay}" Opacity="0.7"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="清空" Padding="12,4" Click="ClearLogs_Click"/>
                    </Grid>
                    <ScrollViewer Grid.Row="1" Margin="0,12,0,0"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="LogsControl" ItemsSource="{x:Bind ViewModel.Logs, Mode=OneWay}" FontFamily="Consolas" FontSize="12">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
