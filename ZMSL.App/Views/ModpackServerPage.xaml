<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ZMSL.App.Views.ModpackServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Background="#FF9800" CornerRadius="8" Width="40" Height="40" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE7B8;" Foreground="White" FontSize="20"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="整合包开服" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="从CurseForge获取或上传本地整合包" FontSize="12" Opacity="0.6"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- 主内容区 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：CurseForge整合包搜索 -->
            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
                        <FontIcon Glyph="&#xE719;" FontSize="20" Foreground="#FF9800"/>
                        <TextBlock Text="CurseForge 整合包" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="OnlineCountText" Text="(0)" Opacity="0.6" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBox x:Name="SearchBox" Grid.Row="1" PlaceholderText="搜索整合包后按回车..." Margin="0,0,0,12"
                             KeyDown="Search_KeyDown"/>

                    <ScrollViewer Grid.Row="2">
                        <ItemsControl x:Name="ModpackList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                            CornerRadius="8" Padding="16" Margin="0,4"
                                            PointerPressed="ModpackItem_Click">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Border CornerRadius="8" Width="48" Height="48" Background="{ThemeResource CardStrokeColorDefaultBrush}">
                                                <Image Source="{Binding IconUrl}" Stretch="UniformToFill"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                                    <TextBlock Text="{Binding Author}" Opacity="0.6" FontSize="11"/>
                                                    <TextBlock Text="{Binding DownloadsText}" Opacity="0.5" FontSize="11"/>
                                                </StackPanel>
                                                <TextBlock Text="{Binding GameVersions}" Opacity="0.5" FontSize="10" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>

                                            <Button Grid.Column="2" Content="选择" Click="SelectModpack_Click"
                                                    Style="{StaticResource AccentButtonStyle}"
                                                    Padding="16,6" FontSize="12" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- 分页控件 -->
                    <Border Grid.Row="3" Margin="0,12,0,0" Padding="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16">
                            <Button x:Name="PrevPageBtn" Click="PrevPage_Click" Padding="12,6" IsEnabled="False">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE76B;" FontSize="12"/>
                                    <TextBlock Text="上一页" FontSize="12"/>
                                </StackPanel>
                            </Button>
                            <TextBlock x:Name="PageInfoText" Text="1 / 1" VerticalAlignment="Center" FontSize="13" Opacity="0.8"/>
                            <Button x:Name="NextPageBtn" Click="NextPage_Click" Padding="12,6" IsEnabled="False">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="下一页" FontSize="12"/>
                                    <FontIcon Glyph="&#xE76C;" FontSize="12"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <ProgressRing x:Name="LoadingRing" Grid.Row="2" IsActive="False" Width="40" Height="40"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>

                    <StackPanel x:Name="EmptyPanel" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                        <FontIcon Glyph="&#xE774;" FontSize="48" Opacity="0.3"/>
                        <TextBlock Text="搜索整合包开始" FontSize="14" Opacity="0.5" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧：配置面板 -->
            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="12" Padding="20">
                <ScrollViewer>
                    <StackPanel Spacing="20">
                        <!-- 整合包来源选择 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="整合包来源" FontWeight="SemiBold"/>
                            <RadioButtons x:Name="SourceRadio" SelectionChanged="Source_Changed">
                                <RadioButton Content="从CurseForge选择" IsChecked="True"/>
                                <RadioButton Content="上传本地整合包"/>
                            </RadioButtons>
                        </StackPanel>

                        <!-- 本地上传区域 -->
                        <StackPanel x:Name="LocalUploadPanel" Spacing="8" Visibility="Collapsed">
                            <TextBlock Text="选择整合包文件" FontWeight="SemiBold"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="LocalFilePathBox" IsReadOnly="True" PlaceholderText="选择 .mrpack 或 .zip 文件"/>
                                <Button Grid.Column="1" Content="浏览" Click="BrowseFile_Click" Margin="8,0,0,0"/>
                            </Grid>
                        </StackPanel>

                        <!-- 已选择的整合包信息 -->
                        <Border x:Name="SelectedModpackPanel" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8" Padding="16" Visibility="Collapsed">
                            <StackPanel Spacing="12">
                                <TextBlock Text="已选择的整合包" FontWeight="SemiBold" Foreground="#FF9800"/>
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <Border CornerRadius="8" Width="64" Height="64" Background="{ThemeResource CardStrokeColorDefaultBrush}">
                                        <Image x:Name="SelectedIconImage" Stretch="UniformToFill"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock x:Name="SelectedNameText" FontWeight="SemiBold" FontSize="16"/>
                                        <TextBlock x:Name="SelectedAuthorText" Opacity="0.6" FontSize="12"/>
                                        <TextBlock x:Name="SelectedGameVersionText" Opacity="0.5" FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- 版本选择 -->
                                <StackPanel x:Name="VersionSelectPanel" Spacing="6" Visibility="Collapsed">
                                    <TextBlock Text="选择服务端版本" FontWeight="SemiBold" FontSize="13"/>
                                    <ComboBox x:Name="VersionComboBox" HorizontalAlignment="Stretch"
                                              SelectionChanged="VersionComboBox_SelectionChanged"
                                              PlaceholderText="选择版本...">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{Binding GameVersionsText}" Opacity="0.6" FontSize="12"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    <TextBlock x:Name="VersionInfoText" Text="" Opacity="0.6" FontSize="11" TextWrapping="Wrap"/>
                                </StackPanel>

                                <TextBlock x:Name="SelectedVersionText" Opacity="0.5" FontSize="11"/>
                            </StackPanel>
                        </Border>

                        <!-- 服务器名称 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="服务器名称" FontWeight="SemiBold"/>
                            <TextBox x:Name="ServerNameBox" PlaceholderText="输入服务器名称"/>
                        </StackPanel>

                        <!-- 内存配置 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="内存配置" FontWeight="SemiBold"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="最小内存 (MB)" FontSize="12" Opacity="0.7"/>
                                    <NumberBox x:Name="MinMemoryBox" Value="2048" Minimum="512" Maximum="32768" SpinButtonPlacementMode="Compact"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="最大内存 (MB)" FontSize="12" Opacity="0.7"/>
                                    <NumberBox x:Name="MaxMemoryBox" Value="4096" Minimum="1024" Maximum="65536" SpinButtonPlacementMode="Compact"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!-- 端口配置 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="服务器端口" FontWeight="SemiBold"/>
                            <NumberBox x:Name="PortBox" Value="25565" Minimum="1024" Maximum="65535" SpinButtonPlacementMode="Compact"/>
                        </StackPanel>

                        <!-- 客户端整合包保存选项 -->
                        <StackPanel Spacing="8">
                            <CheckBox x:Name="SaveClientPackCheck" Content="同时保存客户端整合包到服务器目录" IsChecked="True"/>
                            <TextBlock Text="方便分享给玩家下载" FontSize="11" Opacity="0.5" Margin="28,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- 底部按钮栏 -->
        <Grid Grid.Row="2" Margin="0,24,0,0"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,1,0,0" Padding="16">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Click="Back_Click" Padding="16,10">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE72B;" FontSize="14"/>
                        <TextBlock Text="返回" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button x:Name="CreateButton" Grid.Column="1" Click="Next_Click" Padding="20,10"
                        Style="{StaticResource AccentButtonStyle}" IsEnabled="False">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72A;" FontSize="14"/>
                        <TextBlock Text="下一步" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Page>
