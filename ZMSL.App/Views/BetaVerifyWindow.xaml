<Window
    x:Class="ZMSL.App.Views.BetaVerifyWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="内测验证">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 拖动区域 -->
        <Grid x:Name="AppTitleBar" Height="32">
            <TextBlock Text="ZMSL 内测版验证" VerticalAlignment="Center" Margin="16,0,0,0" Style="{StaticResource CaptionTextBlockStyle}"/>
        </Grid>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="24" Width="400" Margin="0,0,0,32">
            <StackPanel Spacing="8" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE72E;" FontSize="48" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center"/>
                <TextBlock Text="内测版本验证" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="感谢您参与内测体验，请输入您的专属内测卡密以激活使用权限。" 
                           TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Opacity="0.7"/>
            </StackPanel>
            
            <StackPanel Spacing="8">
                <TextBox x:Name="KeyBox" Header="卡密" PlaceholderText="请输入内测卡密" CornerRadius="4"/>
                <TextBlock x:Name="ErrorText" Foreground="Red" Visibility="Collapsed" TextWrapping="Wrap" FontSize="12"/>
            </StackPanel>
            
            <Button x:Name="VerifyButton" Content="验证并启动" Style="{StaticResource AccentButtonStyle}" 
                    HorizontalAlignment="Stretch" Height="36" Click="VerifyButton_Click" CornerRadius="4"/>
            
            <ProgressRing x:Name="LoadingRing" IsActive="False" Visibility="Collapsed" HorizontalAlignment="Center" Width="24" Height="24"/>
            
            <StackPanel Spacing="4">
                <TextBlock Text="如果您遇到问题，请联系管理员获取帮助" FontSize="12" Opacity="0.5" HorizontalAlignment="Center"/>
                <TextBlock x:Name="HwidText" FontSize="10" Opacity="0.3" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
