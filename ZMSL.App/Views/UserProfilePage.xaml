<Page
    x:Class="ZMSL.App.Views.UserProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:ZMSL.App.ViewModels"
    xmlns:converters="using:ZMSL.App.Converters"
    xmlns:dtos="using:ZMSL.Shared.DTOs"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityInverse" Invert="True"/>
        <converters:DateTimeToStringConverter x:Key="DateTimeToString"/>
        <converters:NotificationIconConverter x:Key="NotificationIconConverter"/>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Spacing="24">
                <PersonPicture DisplayName="{x:Bind ViewModel.User.Username, Mode=OneWay}" 
                             ProfilePicture="{x:Bind ViewModel.AvatarImage, Mode=OneWay}"
                             Height="80" Width="80"/>
                
                <StackPanel Spacing="8" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind ViewModel.DisplayName, Mode=OneWay}" 
                             Style="{StaticResource TitleTextBlockStyle}"/>
                    
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                                CornerRadius="4" Padding="8,2">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE735;" FontSize="12"/>
                                <TextBlock Text="{x:Bind ViewModel.User.Level, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Lv.{0}'}" 
                                         FontSize="12"/>
                            </StackPanel>
                        </Border>
                        
                        <TextBlock Text="{x:Bind ViewModel.User.Role, Mode=OneWay}" 
                                 Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    </StackPanel>
                    
                    <!-- Experience Bar (Simple representation) -->
                    <ProgressBar Value="{x:Bind ViewModel.User.Experience, Mode=OneWay}" Maximum="100" Width="200" HorizontalAlignment="Left"/>
                    <TextBlock Text="{x:Bind ViewModel.User.Experience, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Exp: {0}'}" 
                             FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                </StackPanel>
            </StackPanel>
            
            <Button Content="退出登录" Command="{x:Bind ViewModel.LogoutCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,0,0"/>
        </StackPanel>

        <!-- Content -->
        <Pivot Grid.Row="1" SelectedIndex="{x:Bind ViewModel.SelectedPivotIndex, Mode=TwoWay}">
            <!-- Notifications -->
            <!--<PivotItem>
                <PivotItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="站内信"/>
                        <Border Background="Red" CornerRadius="10" MinWidth="20" Height="20" 
                                Visibility="{x:Bind ViewModel.UnreadCount, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                            <TextBlock Text="{x:Bind ViewModel.UnreadCount, Mode=OneWay}" Foreground="White" 
                                     FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4,0"/>
                        </Border>
                    </StackPanel>
                </PivotItem.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                        <AppBarButton Icon="Read" Label="全部已读" Command="{x:Bind ViewModel.MarkAllReadCommand}"/>
                    </CommandBar>
                    
                    <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Notifications}" SelectionMode="None"
                              IsItemClickEnabled="True" ItemClick="NotificationListView_ItemClick">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="dtos:NotificationDto">
                                <Grid Padding="12" Margin="0,4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <FontIcon Glyph="{x:Bind Type, Converter={StaticResource NotificationIconConverter}}" 
                                            FontSize="24" Margin="0,0,16,0" VerticalAlignment="Center"
                                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Content}" TextWrapping="Wrap"/>
                                        <TextBlock Text="{x:Bind CreatedAt, Converter={StaticResource DateTimeToString}}" 
                                                 FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                    
                                    <Border Grid.Column="2" Background="{ThemeResource SystemControlHighlightAccentBrush}" 
                                            Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Margin="8,0,0,0"
                                            Visibility="{x:Bind IsRead, Converter={StaticResource BoolToVisibilityInverse}}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <ProgressRing Grid.Row="1" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" 
                                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"/>
                </Grid>
            </PivotItem>-->
        </Pivot>
    </Grid>
</Page>
